/**
 * @file
 * UEBERBIT behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.ueberbit = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
