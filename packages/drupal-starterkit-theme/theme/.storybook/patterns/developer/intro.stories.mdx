import { Meta, Canvas } from '@storybook/addon-docs';

<Meta title="Developer/Intro"
  parameters={{ viewMode: 'docs', previewTabs: { canvas: { hidden: true }}}}
/>

# Intro

@TODO docs add wingsuit docs

@TODO docs add automagic libraries

@TODO docs add automagic breakpoints

@TODO docs add dist theme inheritance

@TODO docs add storybook config

@TODO docs add polyfill

@TODO unplugin icons

@TODO font loading


## Structure

Vite scans the `/ts`, `/css` and templates directory. Common assets can be placed inside `/css` and `/js`. Components are placed inside `/templates`.

Every source file is used as an entrypoint. Files which are prefix with an underscore are not used as an entrypoint.

